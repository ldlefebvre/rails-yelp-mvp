<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, as: :integer, collection: 1..5 %>
  <%= f.button :submit, "Post Review", class: "btn btn-primary" %>
<% end %>
